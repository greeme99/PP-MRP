# HANDOFF — Prod.Plan 생산계획시스템

작성일: 2026-07-14. 다음 작업자(사람 또는 에이전트)가 이 문서와 `AGENTS.md`만 읽으면
이어서 작업할 수 있도록 현재 상태를 요약한다. 아키텍처·명령어·설계 정책은 `AGENTS.md` 참고.

## 1. 완료된 모듈 (전부 검증 통과 상태)

| 모듈 | 경로 | 상태 |
|---|---|---|
| 기준정보 (품목/BOM/거래처/라인) | `/items` `/partners` `/lines` | 완료. BOM 순환 저장 차단 |
| 수주 관리 | `/orders` | 완료. 라인 삭제 시 계획 연쇄 삭제 |
| 주간 MPS | `/mps` | 완료. 초안 생성(멱등)·셀 편집·부하 색상·수량 불일치 배지 |
| 일별계획 | `/daily` | 완료. 주간→월~금 균등 분할(멱등), 일일 CAPA 부하, 주말 수동 입력 |
| MRP 총소요 | `/mrp` | 완료. 재고 차감 없음(순소요는 재고 모듈 도입 후) |
| 엑셀 가져오기 | `/import` | 완료. 시트 단위 all-or-nothing, 행/열 에러 리포트, 파일 형식 시그니처 진단 |
| 엑셀 내보내기 | MPS/MRP | 완료. 일별계획 export는 없음 |

- 테스트 64개 통과 (`npm test`), typecheck/lint/production build 통과.
- 인증 없음 (사내망 MVP 전제). 도입 시 `src/middleware.ts` 단일 지점.

## 2. 데이터 상태 (prisma/dev.db — git 미포함)

2026-07-14에 실데이터 이관 완료:
- 라인 10개 (일일생산능력 ×5 = 주간 CAPA, dailyCapacity에 원본 일일값 보존. "점검예정" L10만 가동중지)
- 품목 100 / 거래처 50 / BOM 500행 / 수주 1,000건
- 수주 중 과거 납기(7/14 이전) 768건은 CLOSED 처리됨 — 미납품 건이 있으면 수주 화면에서 OPEN으로 복원 필요
- 주간 계획 232건 생성됨. 7/27·8/3 주 L01/L03 CAPA 초과 → 사용자가 그리드에서 조정할 것
- 7/27 주만 일별 분할 완료(205건). 다른 주는 각 주 화면에서 "일별 분할" 실행
- 원본 엑셀의 기존 계획(PlanEntry 시트 400건)은 사용자 결정으로 이관하지 않음

## 3. 저장소 상태

- `main` 브랜치, `09c851f Initial commit` 이후 **이번 세션 변경(MRP, 일별계획, import 형식 진단)이 미커밋** 상태.
- 새 마이그레이션: `prisma/migrations/20260714065145_daily_plan/` (dailyCapacity + DailyPlanEntry).
- 다른 환경에서 최초 셋업: `npm install && npm run db:migrate` (dev.db는 미포함이므로 엑셀 재이관 또는 db 파일 복사 필요).

## 4. 다음 개발 (사용자와 합의된 우선순위)

1. **생산실적 (1순위, 사용자에게 제안됨 — 착수 승인 대기)**
   - `ProductionResult` 모델: 일자·라인·품목(·수주라인)·실적수량·불량수량
   - 일별계획 화면에 실적 행 + 달성률, 미달 잔량 이월 버튼(자동 이월 금지 — 사람이 결정)
   - 실적 입력 시작 시 인증/입력자 기록 도입 검토
2. **재고 + MRP 순소요 (2순위)**: `InventoryTx` 신설 → `/mrp` 총소요를 순소요로 확장
3. **운영 편의 (틈새)**: 일별계획 export(작업지시서), 가져오기 에러 요약 뷰·연쇄 에러 안내, 대시보드

## 5. 알려진 한계·위험 (설계상 의도 포함)

- CAPA는 pcs 단위 — 이기종 혼류 왜곡 (품목군별 라인 분리 운영 전제, `Item.capacityWeight` 확장 여지)
- 수주/주간계획 변경 시 하위 계획이 자동 추종하지 않음 — 불일치 배지 + 초기화 버튼이 공식 흐름
- SQLite 단일 파일 — 백업 절차 미수립 (운영 전 `prisma/dev.db` 정기 백업 필요)
- `window.alert` 금지 등 프로젝트 관례는 `.claude/memory/MEMORY.md` 참고
